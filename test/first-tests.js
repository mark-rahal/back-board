import {describe} from "mocha";

const assert = require('assert');
const createThread = require('/../../routes/index');

describe('Create Thread', function () {
    it('Should give error if incomplete data is given.', function () {
        
    });
});
